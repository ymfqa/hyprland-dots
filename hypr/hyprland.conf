##########
# 显示器 #
##########
# 日用分辨率
monitor = DP-1,2560x1440@180,auto,auto #核显
monitor = DP-3,2560x1440@180,auto,auto,
monitor = eDP-1,2560x1440@165,auto,1.3333334 #笔记本自带独显
# 上课用分辨率
# monitor = HDMI-A-1,1920x1080@60,auto,auto,mirror,eDP-1 #HDMI外接
# monitor = DP-3,1920x1080@60,auto,auto,mirror,eDP-1 #独显
# monitor = eDP-1,1920x1080@60,auto,1 #笔记本自带独显
################
# 一些程序变量 #
################
$terminal = kitty #终端
$fileManager = thunar #文件管理器
$menu = wofi --show drun #程序启动器
$edit = neovide #编辑器
############
# 开机自启 #
############
exec-once = copyq #剪贴板
exec-once = waybar #状态栏
# exec-once = hyprpaper #壁纸软件
exec-once = fcitx5 #输入法
exec-once = mako #消息通知
exec-once = systemctl --user start hyprpolkitagent #图形化输入sudo密码
exec-once = mpvpaper ALL /home/hamster/Videos/wallpaper.MP4 -o "--loop-file=inf --video-zoom=0.1"  #动态壁纸
# 启动 portal
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
############
# 环境变量 #
############
# 鼠标
env = XCURSOR_SIZE,24 #x11的鼠标大小
env = HYPRCURSOR_SIZE,24 #安装了hyprcursor的鼠标大小
# env = HYPRCURSOR_THEME,rose-pine-hyprcursor #鼠标主题
# 中文
env=LANG,zh_CN.UTF-8
env=LANGUAGE,zh_CN:en_US
# nvidia
# env = GBM_BACKENDS,nvidia-drm
# env = GBM_BACKEND,nvidia-drm
# env = LIBVA_DRIVER_NAME,nvidia
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# env = NVD_BACKEND,direct
# tmux
env =TERM,xterm-256color
# fcitx
env = GTK_IM_MODULE,fcitx
env = QT_IM_MODULE,fcitx
env = XMODIFIERS,@im=fcitx
env = SDL_IM_MODULE,fcitx
env = GLFW_IM_MODULE,ibus  # 某些应用（如 Electron）可能需要此设置
# gdk应用
env = GDK_BACKEND,wayland,x11,*
env = GDK_SCALE,1.5
# electron变量
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = ELECTRON_ENABLE_WAYLAND_IME,1
env = ELECTRON_FORCE_WAYLAND,1
############
# 视觉效果 #
############
# 常规
general { 
	# 间距
    gaps_in = 5 #窗口间间距
    gaps_out = 10 #窗口距离屏幕边缘间距
	# 边距
    border_size = 2 #边距宽度
	# 边距颜色
    col.active_border = rgba(BEFAFFdf) rgba(B487FFdf) 55deg
    col.inactive_border = rgba(777777aa)
}
# 装饰
decoration {
    # 圆角
	rounding = 12
    rounding_power = 6
	# 透明度
    active_opacity = 1.0
    inactive_opacity = 1.0
	# 窗口变暗
	dim_inactive=true
	dim_strength=0.05
	# 阴影
    shadow {
        enabled = false
    }
	# 透明度
    blur {
        enabled = true
        size = 3
        passes = 1
		# 优化模糊
		new_optimizations=true
		# 颜色饱和度
        vibrancy = 0.1696
    }
}
# 动画
animations {
    enabled = yes, please :)
	# 贝塞尔曲线（使用这些hypr预定义的就好)
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1
	# hypr预定义的动画（西贝动画)
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}
# 树状分割布局（默认的就是这个）
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}
# 主次布局
master {
    new_status = master
}
# 杂项（用了hyprpaper就用不上了)
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}
########
# 输入 #
########
input {
	# 键盘布局
    kb_layout = us
	# 跟随鼠标(1的话鼠标到哪就激活哪个窗口)
    follow_mouse = 1
	# 鼠标灵敏度
    sensitivity = 0
	# 触摸板
    touchpad {
        natural_scroll = true # 自然滚动（true就和windows一样）
    }
}
# 触摸板手势
# 4指在工作区之间滑动
gesture = 4, horizontal, workspace
# 3指拖动窗口
gesture = 3, swipe, move
# 禁用更新后弹窗
ecosystem {
	no_update_news=true
}
############
# 按键绑定 #
############
$mainMod = SUPER # 主键（win键）
bind = $mainMod, Q, exec, $terminal #打开终端
bind = $mainMod, R, exec, $menu #打开应用菜单
bind = $mainMod, E, exec, $fileManager #打开文件管理器
bind = $mainMod, W, exec, $edit #打开编辑器
bind = $mainMod, C, killactive, #关闭窗口
bind = $mainMod, M, exit, #退出hyprland
bind = $mainMod, Z, togglefloating, #让窗口浮动
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # 切换方向（横着或者竖着分割）
# 窗口之间切换
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
# 工作区之间切换
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
# 将窗口移动到工作区
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
# 开启特殊工作区
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
# 鼠标滚轮切换工作区
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
# 拖动窗口和更改窗口大小
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
# 笔记本的快捷键预设
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
# 剪贴板
bind = $mainMod, V, exec, copyq toggle
# 截屏
bind = ,HOME,exec,hyprshot -z -m region --clipboard-only  
bind = $mainMod, HOME,exec,hyprshot -z -m output --clipboard-only  
# 休眠/锁屏
bind = $mainMod, L, exec,hyprlock  # Win+L 睡眠
############
# 窗口规则 #
############
# 默认的，最好别动
windowrule = suppressevent maximize, class:.*
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
# windowrule  {
#     name = xwayland-video-bridge-fixes
#     match:class = xwaylandvideobridge
#
#     no_initial_focus = true
#     no_focus = true
#     no_anim = true
#     no_blur = true
#     max_size = 1 1
#     opacity = 0.0
# }
############
# xwayland #
############
xwayland {
	# 强制不缩放，避免应用糊了
	force_zero_scaling = true
}
